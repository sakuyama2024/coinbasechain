# ThreadSanitizer suppression file
# These are known false positives or benign races in third-party code

# Catch2 test framework - internal assertion tracking
# Multiple threads calling REQUIRE() trigger benign races in Catch2's internal counters
race:Catch::RunContext::assertionPassed
race:catch_amalgamated.cpp

# spdlog - internal buffer management
# spdlog uses lock-free queues that TSan may flag
# race:spdlog

# Boost.Asio - internal io_context implementation
# Boost uses lock-free techniques that TSan doesn't understand
# race:boost::asio
